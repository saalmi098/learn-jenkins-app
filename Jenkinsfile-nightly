pipeline {
    agent any

    stages {
        stage('Build Docker image') {
            steps {
                sh 'docker build -t my-playwright .' // my-playwright ... name for the image
            }
        }
    }
}